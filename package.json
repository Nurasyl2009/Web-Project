{
  "name": "travelway-project",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/init-db.js && node backend/server.js"
  },
  "dependencies": {
    "react-hot-toast": "^2.6.0"
  }
}

